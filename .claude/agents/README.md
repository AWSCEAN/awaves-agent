# AWAVES Multi-Agent System

Claude Code 기반 멀티 에이전트 개발 워크플로우입니다.

## Agent Architecture

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                              User Request                                     │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   │
                    ┌──────────────▼──────────────┐
                    │      Task Classification     │
                    │   (Frontend / Backend / Both)│
                    └──────────────┬──────────────┘
                                   │
              ┌────────────────────┼────────────────────┐
              │                    │                    │
              ▼                    ▼                    ▼
┌─────────────────────┐  ┌─────────────────┐  ┌─────────────────────┐
│  Frontend Dev Agent │  │  협업 필요 시    │  │  Backend Dev Agent  │
│  ─────────────────  │  │  양쪽 Agent 호출 │  │  ─────────────────  │
│  • apps/web/ 담당   │  │                 │  │  • apps/api/ 담당   │
│  • React/Next.js    │◄─┤  API 계약 협의   ├─►│  • FastAPI/Python   │
│  • UI 컴포넌트      │  │  인터페이스 정의  │  │  • DB/비즈니스 로직 │
└──────────┬──────────┘  └─────────────────┘  └──────────┬──────────┘
           │                                             │
           └──────────────────┬──────────────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │   Review Agent    │
                    │   ─────────────   │
                    │   보안/품질 검증   │
                    │   GO / NO-GO 결정 │
                    └─────────┬─────────┘
                              │
                   ┌──────────┴──────────┐
                   │                     │
            ┌──────▼──────┐       ┌──────▼──────┐
            │   NO-GO     │       │     GO      │
            │   수정 필요  │       │   통과      │
            └──────┬──────┘       └──────┬──────┘
                   │                     │
                   ▼                     ▼
            ┌─────────────┐      ┌─────────────┐
            │ 해당 Dev    │      │  QA Agent   │
            │ Agent로     │      │  ─────────  │
            │ 돌아가기    │      │  테스트 검증 │
            └─────────────┘      └──────┬──────┘
                                        │
                              ┌─────────┴─────────┐
                              │                   │
                       ┌──────▼──────┐     ┌──────▼──────┐
                       │    FAIL     │     │    PASS     │
                       │   테스트    │     │   통과      │
                       │   실패      │     │             │
                       └──────┬──────┘     └──────┬──────┘
                              │                   │
                              ▼                   ▼
                       ┌─────────────┐     ┌─────────────┐
                       │ 해당 Dev    │     │ Docs Agent  │
                       │ Agent로     │     │ ─────────── │
                       │ 돌아가기    │     │ 문서 업데이트│
                       └─────────────┘     └──────┬──────┘
                                                  │
                                                  ▼
                                           ┌─────────────┐
                                           │    User     │
                                           │ Confirmation│
                                           └──────┬──────┘
                                                  │
                                                  ▼
                                           ┌─────────────┐
                                           │ 다음 작업   │
                                           │ 또는 완료   │
                                           └─────────────┘
```

---

## Agents

| Agent | File | 담당 영역 | 역할 |
|-------|------|----------|------|
| **Frontend Dev** | `frontend-dev-agent.md` | `apps/web/` | React/Next.js 개발, UI 구현 |
| **Backend Dev** | `backend-dev-agent.md` | `apps/api/` | FastAPI 개발, API/DB 구현 |
| **Review** | `review-agent.md` | 전체 코드 | 보안/품질 검증, Go/No-Go 결정 |
| **QA** | `qa-agent.md` | 전체 시스템 | 테스트 실행, 엣지 케이스 검증 |
| **Docs** | `docs-agent.md` | `/docs/` | 문서화, 작업 연속성 보장 |

---

## 역할 분리 정책

### 🔴 핵심 규칙

```
┌─────────────────────────────────────────────────────────────────┐
│                    역할 분리 원칙 (CRITICAL)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Frontend Dev Agent                Backend Dev Agent            │
│  ─────────────────                 ─────────────────            │
│  ✅ apps/web/* 수정               ✅ apps/api/* 수정            │
│  ❌ apps/api/* 수정 금지          ❌ apps/web/* 수정 금지        │
│                                                                 │
│  API 변경 필요 시:                 API 구현 완료 시:             │
│  → @backend-dev-agent 요청        → @frontend-dev-agent 알림    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 협업 프로토콜

| 상황 | 요청자 | 수신자 | 액션 |
|------|--------|--------|------|
| 새 API 필요 | Frontend | Backend | API 명세 제안 → 구현 요청 |
| API 구현 완료 | Backend | Frontend | 엔드포인트 정보 전달 |
| API 변경 필요 | Frontend | Backend | 변경 사항 협의 |
| Breaking Change | Backend | Frontend | 사전 알림 → 마이그레이션 가이드 |

### 공유 리소스

| 리소스 | 위치 | 담당 |
|--------|------|------|
| API 명세 | `docs/api.md` | Backend (작성), Frontend (참조) |
| 타입 정의 | `packages/shared/` | 공동 관리 |
| 환경 변수 | `.env.example` | 각자 영역 담당 |

---

## Workflow 상세

### 1️⃣ Frontend Only 작업

```
User: "버튼 색상 변경해줘"
         │
         ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│ Frontend Dev    │────►│ Review Agent    │────►│ QA Agent        │
│ 스타일 수정     │     │ 코드 리뷰       │     │ UI 테스트       │
└─────────────────┘     └─────────────────┘     └────────┬────────┘
                                                         │
                                                         ▼
                                                ┌─────────────────┐
                                                │ Docs Agent      │
                                                │ (필요시)        │
                                                └─────────────────┘
```

### 2️⃣ Backend Only 작업

```
User: "API 응답 캐싱 추가해줘"
         │
         ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│ Backend Dev     │────►│ Review Agent    │────►│ QA Agent        │
│ 캐싱 로직 구현  │     │ 보안/성능 리뷰  │     │ API 테스트      │
└─────────────────┘     └─────────────────┘     └────────┬────────┘
                                                         │
                                                         ▼
                                                ┌─────────────────┐
                                                │ Docs Agent      │
                                                │ API 문서 업데이트│
                                                └─────────────────┘
```

### 3️⃣ Full-Stack 작업 (협업 필요)

```
User: "사용자 프로필 이미지 업로드 기능 추가해줘"
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│                        작업 분석 & 분배                          │
└─────────────────────────┬───────────────────────────────────────┘
                          │
         ┌────────────────┴────────────────┐
         │                                 │
         ▼                                 ▼
┌─────────────────┐               ┌─────────────────┐
│ Backend Dev     │               │ Frontend Dev    │
│ ─────────────── │               │ ─────────────── │
│ 1. S3 업로드 API│◄─── API ────►│ 3. 업로드 UI    │
│ 2. 이미지 처리  │    계약 협의  │ 4. 프리뷰 기능  │
└────────┬────────┘               └────────┬────────┘
         │                                 │
         └────────────────┬────────────────┘
                          │
                          ▼
                 ┌─────────────────┐
                 │ Review Agent    │
                 │ (양쪽 코드 리뷰)│
                 └────────┬────────┘
                          │
                          ▼
                 ┌─────────────────┐
                 │ QA Agent        │
                 │ (E2E 테스트)    │
                 └────────┬────────┘
                          │
                          ▼
                 ┌─────────────────┐
                 │ Docs Agent      │
                 │ (전체 문서화)   │
                 └─────────────────┘
```

---

## Usage in Claude Code (Cursor)

### Agent 호출 방법

```markdown
# Frontend 작업 시
@frontend-dev-agent
[작업 내용]

# Backend 작업 시
@backend-dev-agent
[작업 내용]

# 코드 리뷰 요청 시
@review-agent
Files: [파일 목록]
Change type: [변경 유형]

# 테스트 요청 시
@qa-agent
Feature: [기능 설명]

# 문서화 요청 시
@docs-agent
Update: [업데이트 내용]
```

### Agent 파일 위치

```
.claude/agents/
├── README.md                 # 이 문서
├── frontend-dev-agent.md     # Frontend 개발
├── backend-dev-agent.md      # Backend 개발
├── review-agent.md           # 코드 리뷰
├── qa-agent.md               # QA/테스트
└── docs-agent.md             # 문서화
```

---

## Gate Rules

| Gate | 트리거 | 통과 조건 |
|------|--------|----------|
| **Review Gate** | 모든 코드 변경 | GO 판정 |
| **QA Gate** | 마일스톤 완료 | PASS 판정 |
| **Docs Gate** | 기능/API 변경 | 문서 동기화 완료 |

---

## Quick Reference

```
Frontend 작업 → @frontend-dev-agent → @review-agent → @qa-agent → @docs-agent
Backend 작업  → @backend-dev-agent  → @review-agent → @qa-agent → @docs-agent
Full-Stack    → 양쪽 Dev Agent 협업 → @review-agent → @qa-agent → @docs-agent
```
