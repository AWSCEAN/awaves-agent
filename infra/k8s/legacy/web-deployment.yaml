apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-web
  namespace: web
  labels:
    app: react-web
    app.kubernetes.io/part-of: awaves
spec:
  replicas: 2
  selector:
    matchLabels:
      app: react-web
  template:
    metadata:
      labels:
        app: react-web
    spec:
      # IRSA: grants the pod AWS permissions without static keys.
      # Attach the following policies to the IAM role:
      #   - AmazonS3ReadOnlyAccess             (read static assets from S3 bucket)
      #   - cloudfront:CreateInvalidation      (cache invalidation on deploy — inline policy)
      #   - CloudWatchFullAccess               (CloudWatch RUM metrics)
      # Created via: eksctl create iamserviceaccount --name awaves-web-sa --namespace web \
      #   --attach-policy-arn <each policy ARN above> --approve
      serviceAccountName: awaves-web-sa
      containers:
        - name: web
          image: 107570140649.dkr.ecr.us-east-1.amazonaws.com/awaves-dev-web-app:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
            # NEXT_PUBLIC_* vars below are informational only — their values are
            # already baked into the JS bundles at docker build time via --build-arg.
            # Changing them here has no effect on client-side code.
            - name: NEXT_PUBLIC_API_URL
              value: "https://api.awaves.net"
            - name: NEXT_PUBLIC_CDN_URL
              value: "https://cdn.awaves.net"
            - name: NEXT_PUBLIC_MAPBOX_TOKEN
              value: "pk.eyJ1IjoicWtkZWtkbmpzMTIwMSIsImEiOiJjbWw0ODc2ejMwejF4M3JzNWhxczc3dHN0In0.wxEvng2hYWnoIY0Yrqk0cA"
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
      imagePullSecrets:
        - name: ecr-secret
