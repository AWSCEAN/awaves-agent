apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: awaves-web-secrets
  namespace: web
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: awaves-web-secrets           # K8s Secret name referenced by web-deployment
    creationPolicy: Owner
  data:
    # Mapbox API token
    - secretKey: mapbox-token
      remoteRef:
        key: awaves/prod/external
        property: mapbox_token
